# Tasks

- [ ] Persist job state in Postgres.
- [ ] Move job ids to non-enumerable identifiers.
- [ ] Implement stale job reconciliation on backend startup.
- [ ] Enforce terminal-state invariants in repository/service code.
- [ ] Implement protected `GET /api/jobs/{job_id}` owner/operator access.
- [ ] Add integration tests for restart and reconciliation behavior.
- [ ] Update `docs/current/alpha-launch/status.md` when PR starts and when it finishes.
