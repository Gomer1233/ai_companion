# Tasks

- [ ] Reconcile ALPHA-012 as `Done` after it is merged.
- [ ] Add backend tests for Mini App image job start with owner-scoped access.
- [ ] Add backend tests for explicit image consent and usage-limit rejection.
- [ ] Add backend tests for job status, cancel, retry, and terminal-state protection.
- [ ] Implement Mini App image job API only by reusing existing job lifecycle services/repositories.
- [ ] Extend Mini App API client with image job start/status/cancel calls.
- [ ] Add frontend tests for queued, running, completed, failed, cancelled, retry, and locked states.
- [ ] Implement image job UI inside the active persona chat/thread.
- [ ] Run backend verification for touched job and access-policy scope.
- [ ] Run `npm test`, `npm run typecheck`, and `npm run build` in `apps/miniapp`.
- [ ] Update `docs/current/alpha-launch/status.md` when implementation is ready for review.
