# Tasks

- [x] Add `ruff` configuration.
- [x] Add limited `mypy` configuration for extracted code.
- [x] Add CI steps for tests and lint.
- [x] Clean active source tree and remove stale references.
- [x] Decide the fate of `commit_changes.ps1` and move or archive it out of the active root path.
- [x] Decide the fate of `system/` and remove it from the active runtime path.
- [x] Remove or isolate `?????/` from the active project route.
- [x] Resolve the mismatch between root `Photo/` and the runtime photo path used by `src/main.py` and `src/bot_lika.py`.
- [x] Reduce the repository root to operational files, manifests, and canonical guidance.
- [x] Update root README for architecture-as-built.
- [x] Verify docs point to the implemented system, not the legacy plan.
